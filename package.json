{
  "name": "ea-exercise-mongoose",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "linter": "npx oxlint@latest"
  },
  "dependencies": {
    "mongoose": "^6.13.8",
    "pino": "^9.12.0"
  },
  "devDependencies": {
    "pino-pretty": "^13.1.1"
  }
}
